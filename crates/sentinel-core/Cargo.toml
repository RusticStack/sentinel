[package]
name = "sentinel-core"
description = "Stable identifiers, job/run state machine, failure classes and cancellation contract"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
uuid = { version = "1.24", default-features = false, features = ["v4", "std"] }

[lints]
workspace = true
